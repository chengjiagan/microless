apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: url-shorten
  labels:
    microless: social-network
    app.kubernetes.io/name: url-shorten
    app.kubernetes.io/instance: kn-deploy
    app.kubernetes.io/component: url-shorten-service
    app.kubernetes.io/part-of: social-network
    networking.knative.dev/visibility: cluster-local
  namespace: social-network-kn
spec:
  template:
    spec:
      containers:
      - name: server
        image: waterlemongan/microless-socialnetwork-urlshorten:0.1.1
        ports:
        - name: h2c
          containerPort: 8080
        env:
        - name: SERVICE_CONFIG
          value: "/config/config.json"
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
      volumes:
        - name: config
          configMap:
            name: config
            items:
            - key: config.json
              path: config.json