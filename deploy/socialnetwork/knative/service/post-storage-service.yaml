apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: post-storage
  labels:
    microless: social-network
    app.kubernetes.io/name: post-storage
    app.kubernetes.io/instance: kn-deploy
    app.kubernetes.io/component: post-storage-service
    app.kubernetes.io/part-of: social-network
    networking.knative.dev/visibility: cluster-local
  namespace: social-network-kn
spec:
  template:
    spec:
      containers:
      - name: server
        image: waterlemongan/microless-socialnetwork-poststorage:0.1.6
        ports:
        - name: h2c
          containerPort: 8080
        env:
        - name: SERVICE_CONFIG
          value: "/config/config.json"
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
      volumes:
        - name: config
          configMap:
            name: config
            items:
            - key: config.json
              path: config.json