apiVersion: v1
kind: Service
metadata:
  name: text-vm
  namespace: social-network
spec:
  clusterIP: None
  selector:
    app: text
    type: vm
  ports:
  - name: grpc
    port: 8080
    targetPort: grpc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: text-vm
  namespace: social-network
spec:
  selector:
    matchLabels:
      app: text
      type: vm
  template:
    metadata:
      labels:
        app: text
        type: vm
      name: text-vm
      namespace: social-network
    spec:
      nodeSelector:
        type: vm
      containers:
      - name: server
        image: waterlemongan/microless-socialnetwork-text:0.2.3
        ports:
        - name: grpc
          containerPort: 8080
        - name: stats
          containerPort: 9000
        env:
        - name: SERVICE_CONFIG
          value: "/config/config.json"
        - name: LB_CONFIG
          value: "/config/lb.json"
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
        resources:
          limits:
            cpu: "1"
            memory: 50Mi
          requests:
            cpu: "1"
            memory: 50Mi
      volumes:
        - name: config
          configMap:
            name: config
            items:
            - key: config.json
              path: config.json
            - key: lb.json
              path: lb.json
