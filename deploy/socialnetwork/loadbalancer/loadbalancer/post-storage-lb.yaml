apiVersion: v1
kind: Service
metadata:
  name: post-storage
  labels:
    microless: social-network
    app.kubernetes.io/name: post-storage
    app.kubernetes.io/instance: k8s-deploy
    app.kubernetes.io/component: post-storage-lb-service
    app.kubernetes.io/part-of: social-network
  namespace: social-network-lb
spec:
  ports:
  - port: 8080
    name: grpc
    targetPort: grpc
  selector:
    microless: social-network
    app.kubernetes.io/name: post-storage
    app.kubernetes.io/instance: k8s-deploy
    app.kubernetes.io/component: post-storage-lb
    app.kubernetes.io/part-of: social-network
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-storage
  labels:
    microless: social-network
    app.kubernetes.io/name: post-storage
    app.kubernetes.io/instance: k8s-deploy
    app.kubernetes.io/component: post-storage-lb-deployment
    app.kubernetes.io/part-of: social-network
  namespace: social-network-lb
spec:
  selector:
    matchLabels:
      microless: social-network
      app.kubernetes.io/name: post-storage
      app.kubernetes.io/instance: k8s-deploy
      app.kubernetes.io/component: post-storage-lb
      app.kubernetes.io/part-of: social-network
  template:
    metadata:
      labels:
        microless: social-network
        app.kubernetes.io/name: post-storage
        app.kubernetes.io/instance: k8s-deploy
        app.kubernetes.io/component: post-storage-lb
        app.kubernetes.io/part-of: social-network
      name: post-storage
    spec:
      containers:
      - name: server
        image: waterlemongan/microless-loadbalancer:0.1.0
        ports:
        - name: grpc
          containerPort: 8080
        env:
        - name: LOADBALANCER_CONFIG
          value: "/config/config.json"
        volumeMounts:
        - name: post-storage-config-lb
          mountPath: "/config"
          readOnly: true
      volumes:
        - name: post-storage-config-lb
          configMap:
            name: post-storage-config-lb
            items:
            - key: config.json
              path: config.json
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: post-storage-config-lb
  namespace: social-network-lb
data:
  config.json: |
    {
      "grpc": ":8080",
      "http": ":8081",
      "backend": {
        "vm": "post-storage.social-network-lb-vm:8080",
        "serverless": "post-storage.social-network-lb-kn.svc.cluster.local:80"
      },
      "deployment": {
        "namespace": "social-network-lb-vm",
        "name": "post-storage"
      },
      "rate_per_pod": 1000
    }
