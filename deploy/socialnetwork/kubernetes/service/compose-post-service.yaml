apiVersion: v1
kind: Service
metadata:
  name: compose-post
  namespace: social-network
spec:
  ports:
  - port: 8080
    name: grpc
    targetPort: grpc
  selector:
    app: compose-post
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: compose-post
  namespace: social-network
spec:
  selector:
    matchLabels:
      app: compose-post
  template:
    metadata:
      labels:
        app: compose-post
      name: compose-post
      namespace: social-network
    spec:
      containers:
      - name: server
        image: waterlemongan/microless-socialnetwork-composepost:0.1.8
        ports:
        - name: grpc
          containerPort: 8080
        env:
        - name: SERVICE_CONFIG
          value: "/config/config.json"
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
      volumes:
        - name: config
          configMap:
            name: config
            items:
            - key: config.json
              path: config.json
