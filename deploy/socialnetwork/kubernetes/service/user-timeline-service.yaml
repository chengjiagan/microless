apiVersion: v1
kind: Service
metadata:
  name: user-timeline
  namespace: social-network
spec:
  ports:
  - port: 8080
    name: grpc
    targetPort: grpc
  selector:
    app: user-timeline
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-timeline
  namespace: social-network
spec:
  selector:
    matchLabels:
      app: user-timeline
  template:
    metadata:
      labels:
        app: user-timeline
      name: user-timeline
      namespace: social-network
    spec:
      containers:
      - name: usertimeline
        image: waterlemongan/microless-socialnetwork-usertimeline:0.1.8
        ports:
        - name: grpc
          containerPort: 8080
        env:
        - name: SERVICE_CONFIG
          value: "/config/config.json"
        volumeMounts:
        - name: config
          mountPath: "/config"
          readOnly: true
      volumes:
        - name: config
          configMap:
            name: config
            items:
            - key: config.json
              path: config.json
